spring:
  application:
    name: config-client
  cloud:
    config:
      #uri: http://localhost:9102
      name: config-demo
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server